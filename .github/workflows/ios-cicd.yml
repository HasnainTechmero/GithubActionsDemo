name: iOS CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@main

   # - name: Set up Xcode
   #   uses: actions/setup-xcode@main
   #   with:
   #     xcode-version: '15.0'

   # - name: Install dependencies
   #   run: |
#        gem install bundler
    #    bundle install
    #    pod install
     # working-directory: /Users/techbaroda/Projects/GithubActionsDemo_App/GithubActionsDemo/GithubActionsDemo.xcodeproj

    - name: Build and Test
      run: xcodebuild -workspace GithubActionsDemo.xcodeproj -scheme GithubActionsDemo -destination "platform=iOS Simulator,OS=16.0,name=iPhone 13" test

   # - name: Deploy
      #uses: actions/setup-xcode@main
      # Add your deployment steps here, e.g., uploading to App Store Connect or TestFlight
